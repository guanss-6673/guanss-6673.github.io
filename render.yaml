services:
  - type: web
    name: scheduling-demo-form
    runtime: python
    buildCommand: echo "no build required"
    startCommand: python3 CODE/run_scheduling_demo.py --host 0.0.0.0 --port $PORT
